#include<stdio.h>
main()
{
	int l,b,a,p;
	printf("enter the length:");
	scanf("%d",&l);
	printf("enter the breadth:");
	scanf("%d",&b);
	a=l*b;
	p=2*(l+b);
	printf("area of a rectangle is %d", a);
	printf("\nperimeter of a rectangle is %d", p);
	

	
	
}
